[pytest]
asyncio_mode = auto
markers =
    unit: fast unit tests
    integration: API and worker integration tests
    fairness: aggregator and fairness metric tests
    load: latency and backlog tests
addopts = --cov=backend --cov-report=term-missing --cov-fail-under=80 --strict-markers
